Place this folder into your Halo 2 Editing Kit "tags" directory.

Place everything from the dw3 "scenarios" directory into the main H2EK scenarios directory

Place everything from the dw3 "data" directory into the main H2EK data directory

Place everything fromthe dw3 "globals" folder into the main global tags directory

Leave everything else within this folder, as the scenarios, tags, etc reference this dw3 directory. should be plug in and play.